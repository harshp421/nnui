// Prerender all pages at build time for SEO
// Every page gets real HTML that search engines can crawl
export const prerender = true;
export const trailingSlash = 'always'; // Helps Cloudflare's routing
